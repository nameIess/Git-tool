name: Bug Report 🐛
description: Report bugs and issues for Git-tool – Help us improve by sharing detailed problem reports.
assignees: [nameIess]
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        ### Thank you for reporting a bug! 
        Please provide as much detail as possible so we can resolve the issue in Git-tool quickly.

  - type: dropdown
    id: severity
    attributes:
      label: 🎯 Impact & Severity
      description: "How significantly does this bug affect your use of the project?"
      options:
        - "Blocks usage completely ❌"
        - "Partial functionality affected ⚠️"
        - "Minor UI/UX issue or typo 🟡"
    validations:
      required: true

  - type: textarea
    id: problem
    attributes:
      label: 🧩 Problem Description
      description: "Clearly describe the bug. What were you doing when it occurred?"
      placeholder: "e.g., The application crashes when I try to save a file with special characters in the name."
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: 🔁 Steps to Reproduce
      description: "List the exact steps needed to reproduce the error."
      placeholder: |
        1. Open Git-tool
        2. Navigate to 'Settings'
        3. Change 'Theme' to 'Dark'
        4. Observe the crash
    validations:
      required: true

  - type: textarea
    id: behavior
    attributes:
      label: 📤 Actual vs Expected Behavior
      description: "Contrast the result you wanted with the result you actually received."
      placeholder: |
        Expected: The theme changes to dark mode without issue.
        Actual: The application window closes immediately.
    validations:
      required: false

  - type: textarea
    id: logs
    attributes:
      label: 🧪 Logs / Error Output
      description: "Paste terminal output, stack traces, or error logs here."
      render: shell
    validations:
      required: false

  - type: textarea
    id: environment
    attributes:
      label: 💻 Environment Details
      description: "Specify your setup to help us debug."
      placeholder: |
        - OS: [e.g. Ubuntu 22.04, Windows 11]
        - Project Version: [e.g. v1.2.0]
        - Relevant Dependencies: [e.g. Docker v24.0, Node v20]
    validations:
      required: true